---

// Import utilities
import { getCollection } from "astro:content";

const blogPosts = await getCollection("blog", ({data}) => {
  return !data.draft;
});


---
<div class="post-card">

  <div class="left">

    {
      blogPosts.map(post  => (
        <h1><a href={post.slug}>{post.data.title}</a></h1>
        <p>{post.data.description}</p>
        <a href={post.slug}>. . . Read More</a>
      ))
    }
   

  </div>
  
  <div class="right">

    {
      blogPosts.map(post  => (
      <a href={post.slug}>
        <img src={post.data.image.src} alt={post.data.image.alt}/>
      </a>
      ))
    }
      
  </div>

</div>

<style>
  .post-card {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: first ;
    
    
  }

  .left {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  h1 {
    font-family: "Barlow Condensed", sans-serif;
    font-size: 4rem;
    font-weight: 600;
    color: #B6E851;


  }

  p {
    font-family: ;
    font-size: 1.2rem;

    color: white;

    width: 40ch;
  }


  img {
    width: 25rem;
    height: auto;
    border: #B6E851 .5rem solid;
  }

  a {
    font-family: ;
    font-size: 1.2rem;

    color: #B6E851;

  }

  a:visited{
    text-decoration: none;
    color: inherit;
  }
  
</style>

