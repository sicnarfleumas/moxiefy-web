---
// Import Utility
import { Image } from "astro:assets";


// const { post} = Astro.props;

// const { title, image, description } = post.frontmatter;


---
<div class="post-card">

  <div class="left">

    
    <!-- <h1 transition:name="blog_title"> <a href={post.url}>  {title} </a> </h1>

    <p>{description}... <span><a href={post.url}>Read more</a></span></p> -->

  </div>
  
  <div class="right">

    <!-- <a href={post.url}>

      <Image
        src={image.src} 
        alt={image.alt}
        class="post-image"
        width="100"
        height="100"
      
      transition:name="blog-image"
      />

    </a> -->
      
  </div>

</div>

<style>
  .post-card {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  }

  .left {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .left > h1 {
    font-family: ;
    font-size: 1.5rem;

    color: #B6E851;

    width: 40ch;

    line-height: 1.25;
  }

  .left > p {
    font-family: ;
    font-size: 1.2rem;

    color: white;

    width: 40ch;
  }

  p > span {
    text-decoration: underline;
  }

  .post-image {
    width: 60%;
    height: auto;
    border: #B6E851 solid 1px;
  }

  a:visited{
    text-decoration: none;
    color: inherit;
  }
  
</style>

